{
    "name": "@cantrip-ui/core",
    "version": "0.0.0",
    "description": "A small framework for creating reactive UIs using just typescript",
    "keywords": ["ui", "reactive", "typescript"],
    "homepage": "https://github.com/schultzcole/cantrip-ui",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+ssh://git@github.com/schultzcole/cantrip-ui.git"
    },
    "type": "module",
    "files": [
        "./src"
    ],
    "exports": {
        ".": {
            "import": "./src/index.mjs",
            "types": "./src/index.d.mts"
        }
    },
    "dependencies": {
        "type-fest": "^5.7.0"
    },
    "devDependencies": {
        "happy-dom": "^20.10.2",
        "vite-plus": "latest"
    },
    "scripts": {
        "clean-pkg": "mkdir -p dist && cp package.json dist/package.json && cd dist && npm pkg delete scripts devDependencies",
        "prepack": "vp pack && vp run clean-pkg && cp LICENSE dist/LICENSE ",
        "pack": "mkdir -p pack && npm pack ./dist --pack-destination ./pack",
        "pub": "vp run prepack && npm publish --access public ./dist",
        "clean": "rm -rf dist && rm -rf pack"
    }
}
